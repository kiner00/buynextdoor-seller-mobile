node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are generated by `expo prebuild` — never hand-edited, so
# never committed. Config lives in app.json + config plugins.
/ios
/android

*.log
npm-debug.*
yarn-debug.*
yarn-error.*

*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

.env
.env.local
.env.*.local

.DS_Store
